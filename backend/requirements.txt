# ── 2026-08-03 memory trim (Brock cost cut) ──────────────────────────
# Removed to shrink container from ~6 GB → ~2.5 GB:
#   PyMuPDF   — PDF parsing (used only in tax.py, lazy import)
#   anthropic — Anthropic LLM client (used only in llm_client.py + aqa.py)
#   simfin    — fundamentals data (used only in quality_momentum_screen)
#   scipy     — scientific computing (transitive dep of sklearn/statsmodels)
#   scikit-learn — ML models (used only in allocator.py LedoitWolf)
#   statsmodels — stat models (used only in factor_attribution + frac_diff)
#   arch      — GARCH/volatility models (grepped: no importers)
#
# All 5 remaining importers use `import X` INSIDE function bodies (lazy),
# so removing the packages does NOT crash the module at load time. The
# features that use them will raise ImportError only if called.
#
# To restore: uncomment the package line + redeploy.
# ─────────────────────────────────────────────────────────────────────
fastapi==0.115.5
sentry-sdk[fastapi]==2.18.0
uvicorn[standard]==0.32.1
sqlalchemy==2.0.36
alembic==1.14.0
pydantic-settings==2.6.1
alpaca-py==0.39.4
pandas==2.2.3
ta==0.11.0
numpy==1.26.4
apscheduler==3.10.4
httpx==0.27.2
python-dotenv==1.0.1
orjson==3.10.11
pytz==2024.2
websockets>=14.0
yfinance>=0.2.61
curl_cffi>=0.7.0
python-jose[cryptography]==3.3.0
bcrypt==4.2.1
stripe>=8.0.0
ccxt>=4.3.0
python-multipart>=0.0.9
# PyMuPDF>=1.24.0         # PAUSED 2026-08-03 (memory) — tax PDF parsing
# anthropic>=0.40.0       # PAUSED 2026-08-03 (memory) — LLM Analyst
PyYAML>=6.0
psycopg2-binary>=2.9.0
beautifulsoup4>=4.12.0
# simfin>=1.0.0           # PAUSED 2026-08-03 (memory) — fundamentals
pandas_datareader>=0.10.0
psutil>=5.9.0          # 2026-08-13 — RSS instrumentation for OOM diagnosis
# scipy>=1.11.0           # PAUSED 2026-08-03 (memory) — pulled in by sklearn/statsmodels only
# scikit-learn>=1.4.0     # PAUSED 2026-08-03 (memory) — allocator.LedoitWolf
# statsmodels>=0.14.0     # PAUSED 2026-08-03 (memory) — factor attribution + frac_diff
# arch>=7.0.0             # PAUSED 2026-08-03 (memory) — GARCH models (no active importers)
